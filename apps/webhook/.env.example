# ============================================================================
# Webhook Bridge Environment Variables (Standalone Deployment)
# ============================================================================
#
# IMPORTANT: For monorepo deployment, use the root .env file instead!
# This file is only for standalone deployment of the webhook service.
#
# See root .env.example for all available variables.
# ============================================================================

# Search Bridge Configuration
# Copy this file to .env and update with your values

# API Server
SEARCH_BRIDGE_HOST=0.0.0.0
SEARCH_BRIDGE_PORT=52100

# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/webhook

# Redis
REDIS_URL=redis://localhost:6379

# Text Embeddings Inference (TEI)
TEI_URL=http://localhost:50200

# Qdrant Vector Database
QDRANT_URL=http://localhost:50201

# Embedding Model
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B
EMBEDDING_DIMENSIONS=1024

# BM25 Search Configuration
BM25_K1=1.5
BM25_B=0.75

# Hybrid Search Weights
HYBRID_VECTOR_WEIGHT=0.7
HYBRID_BM25_WEIGHT=0.3
