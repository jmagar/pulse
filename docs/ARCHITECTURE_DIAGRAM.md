# Docker Compose Architecture Diagram

## Service Topology & Dependencies

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Docker Network: "firecrawl"                      │
│                          (Bridge, Isolated)                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │                    INFRASTRUCTURE LAYER                      │      │
│  │                  (No internal dependencies)                  │      │
│  ├──────────────────────────────────────────────────────────────┤      │
│  │                                                              │      │
│  │  ┌────────────────┐     ┌────────────────┐                 │      │
│  │  │ pulse_postgres   │     │ pulse_redis│                 │      │
│  │  │ (PostgreSQL)   │     │ (Redis)        │                 │      │
│  │  │ Port: 5432     │     │ Port: 6379     │                 │      │
│  │  │ Ext:  50105    │     │ Ext:  50104    │                 │      │
│  │  └────────────────┘     └────────────────┘                 │      │
│  │                                                              │      │
│  │  ┌────────────────────────────────┐                         │      │
│  │  │ pulse_playwright           │                         │      │
│  │  │ (Browser Automation)           │                         │      │
│  │  │ Port: 3000 | Ext: 50100        │                         │      │
│  │  └────────────────────────────────┘                         │      │
│  │                                                              │      │
│  └──────────────────────────────────────────────────────────────┘      │
│           ▲                                                             │
│           │ (depends_on these services)                                │
│           │                                                             │
│  ┌────────┴──────────────────────────────────────────────────┐        │
│  │             PRIMARY SERVICES LAYER                        │        │
│  │        (Depends on Infrastructure Layer)                  │        │
│  ├───────────────────────────────────────────────────────────┤        │
│  │                                                            │        │
│  │  ┌─────────────────────────┐                             │        │
│  │  │ firecrawl               │                             │        │
│  │  │ (Firecrawl API)         │                             │        │
│  │  │ Node.js                 │                             │        │
│  │  │ Port: 3002              │                             │        │
│  │  │ Ext:  50102             │                             │        │
│  │  │ depends_on:             │                             │        │
│  │  │  - pulse_postgres         │                             │        │
│  │  │  - pulse_redis      │                             │        │
│  │  │  - pulse_playwright │                             │        │
│  │  └────────────┬────────────┘                             │        │
│  │               │                                           │        │
│  │  ┌────────────▼──────────────┐                            │        │
│  │  │ pulse_webhook         │                            │        │
│  │  │ (Webhook Bridge / Search) │                            │        │
│  │  │ Python 3.13               │                            │        │
│  │  │ Port: 52100               │                            │        │
│  │  │ Ext:  50108               │                            │        │
│  │  │ depends_on:               │                            │        │
│  │  │  - pulse_postgres           │                            │        │
│  │  │  - pulse_redis        │                            │        │
│  │  └───────────────────────────┘                            │        │
│  │                                                            │        │
│  └────────────────────────────────────────────────────────────┘        │
│           ▲                                                             │
│           │ (depends_on firecrawl)                                     │
│           │                                                             │
│  ┌────────┴──────────────────────────────────────────────────┐        │
│  │          INTEGRATION SERVICES LAYER                       │        │
│  │       (Depends on Primary Services)                       │        │
│  ├───────────────────────────────────────────────────────────┤        │
│  │                                                            │        │
│  │  ┌───────────────────────────┐                            │        │
│  │  │ pulse_mcp             │                            │        │
│  │  │ (MCP Server)              │                            │        │
│  │  │ Node.js / TypeScript      │                            │        │
│  │  │ Port: 3060                │                            │        │
│  │  │ Ext:  50107               │                            │        │
│  │  │ depends_on:               │                            │        │
│  │  │  - firecrawl              │                            │        │
│  │  │ Health Check: ✅ Configured │                            │        │
│  │  └───────────────────────────┘                            │        │
│  │                                                            │        │
│  └────────────────────────────────────────────────────────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│               EXTERNAL SERVICES (GPU Machine / Optional)                │
│              (Separate Docker Context: "docker-compose.external.yaml")  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────┐   ┌──────────────────────────────┐   │
│  │ tei                          │   │ qdrant                      │   │
│  │ (Embeddings / TEI)           │   │ (Vector Database)            │   │
│  │ HuggingFace                  │   │ Qdrant                       │   │
│  │ Port: 80                     │   │ HTTP: 6333 | Ext: 50201      │   │
│  │ Ext:  50200                  │   │ gRPC: 6334 | Ext: 50202      │   │
│  │ Requires: GPU (NVIDIA)       │   │                              │   │
│  │                              │   │                              │   │
│  │ Model: Qwen/Qwen3-Embedding  │   │ Collection: pulse_docs       │   │
│  │ 0.6B (1024 dims)             │   │ (1024-dim vectors, cosine)   │   │
│  └──────────────────────────────┘   └──────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│           CANDIDATE SERVICE (To Be Added)                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────┐                                  │
│  │ pulse_change-detection        │                                  │
│  │ (Change Detection Monitor)       │                                  │
│  │ Python / Flask                   │                                  │
│  │ Port: 5000                       │                                  │
│  │ Ext:  50111 (RECOMMENDED)        │                                  │
│  │                                  │                                  │
│  │ Uses:                            │                                  │
│  │  - pulse_playwright (shared) │                                  │
│  │                                  │                                  │
│  │ Sends to:                        │                                  │
│  │  - pulse_webhook (indexing)  │                                  │
│  └──────────────────────────────────┘                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## Port Allocation Map

```
┌────────────────────────────────────────────────────────────────┐
│                    PORT ALLOCATION MAP                         │
├────────┬──────────────────────────┬──────────────┬─────────────┤
│ Port   │ Service Name             │ Container    │ Status      │
├────────┼──────────────────────────┼──────────────┼─────────────┤
│ 50100  │ Playwright Service       │ playwright   │ ACTIVE      │
│ 50101  │ (Internal/Reserved)      │ firecrawl    │ INTERNAL    │
│ 50102  │ Firecrawl API            │ firecrawl    │ ACTIVE      │
│ 50103  │ (Worker/Reserved)        │ firecrawl    │ INTERNAL    │
│ 50104  │ Redis Cache              │ cache        │ ACTIVE      │
│ 50105  │ PostgreSQL Database      │ db           │ ACTIVE      │
│ 50106  │ (Extract Worker/Res.)    │ firecrawl    │ INTERNAL    │
│ 50107  │ MCP Server               │ mcp          │ ACTIVE      │
│ 50108  │ Webhook Bridge / Search  │ webhook      │ ACTIVE      │
│ 50109  │ >>> AVAILABLE <<<        │ -            │ NEXT        │
│ 50110  │ >>> AVAILABLE <<<        │ -            │ BACKUP      │
│ 50200  │ TEI (Embeddings)         │ tei          │ EXTERNAL    │
│ 50201  │ Qdrant (HTTP)            │ qdrant       │ EXTERNAL    │
│ 50202  │ Qdrant (gRPC)            │ qdrant       │ EXTERNAL    │
└────────┴──────────────────────────┴──────────────┴─────────────┘
```

## Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW LAYERS                             │
└──────────────────────────────────────────────────────────────────────┘

External Internet / Firecrawl API Client
        │
        ▼
┌──────────────────────────────────────────────────────────────────┐
│ firecrawl (API)                  PORT 50102                       │
│ - Receives scrape requests                                        │
│ - Routes to Playwright for JS rendering                           │
│ - Stores results in PostgreSQL                                    │
│ - Queues jobs via Redis                                           │
│ - Triggers webhook on completion                                  │
└─────────┬────────────────┬──────────────┬──────────────┬──────────┘
          │                │              │              │
          ▼                ▼              ▼              ▼
      [Database]      [Cache]      [Playwright]    [Webhook]
      (PostgreSQL)    (Redis)      (Browser)       (HTTP POST)
      50105            50104       50100           52100 (internal)
          │                │              │              │
          └────────────────┴──────────────┴──────────────┘
                           │
                           ▼
            ┌──────────────────────────────────────┐
            │ pulse_webhook (Search Bridge)    │
            │ - Receives webhook notifications    │
            │ - Indexes content (BM25 + Vector)   │
            │ - Stores index on disk              │
            │ PORT 50108                          │
            └──────────────┬───────────────────────┘
                           │
                ┌──────────┴──────────┐
                ▼                     ▼
          [BM25 Index]          [Qdrant Vector DB]
          (Keyword Search)      (Semantic Search)
          On-Disk               50201 (external)
                │                     │
                └──────────────┬──────┘
                               │
                               ▼
            ┌──────────────────────────────────────┐
            │ pulse_mcp (MCP Server)           │
            │ - Registers scrape, search tools     │
            │ - Calls Firecrawl API                │
            │ - Queries webhook indexes            │
            │ PORT 50107                           │
            │ - Health: ✅ Configured               │
            └──────────────┬───────────────────────┘
                           │
                           ▼
                      Claude / Agent
                      (MCP Client)
```

## Volume & Persistence Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│              PERSISTENT STORAGE ARCHITECTURE                     │
│         Base: ${APPDATA_BASE:-/mnt/cache/appdata}               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Host Filesystem                 Container Mount Points         │
│  ────────────────────────────────────────────────              │
│                                                                 │
│  /mnt/cache/appdata/                                            │
│  │                                                              │
│  ├── pulse_postgres/         → /var/lib/postgresql/data    │
│  │   (Database files)            (PostgreSQL)                   │
│  │   └─ RISK: LOW (backups)                                    │
│  │                                                              │
│  ├── pulse_redis/            → /data                       │
│  │   (RDB + AOF logs)            (Redis)                        │
│  │   └─ RISK: MEDIUM (ephemeral) │                             │
│  │                                                              │
│  ├── pulse_mcp_resources/    → /app/resources              │
│  │   (Cached content)            (MCP Server)                   │
│  │   └─ RISK: MEDIUM (TTL 24h)                                 │
│  │                                                              │
│  ├── pulse_webhook/          → /app/data/bm25              │
│  │   (BM25 index)                (Webhook Bridge)               │
│  │   └─ RISK: MEDIUM (rebuild)                                 │
│  │                                                              │
│  ├── firecrawl_tei_data/         → /data                       │
│  │   (Model cache)               (TEI - External)               │
│  │   └─ RISK: LOW (reproducible)                               │
│  │                                                              │
│  └── firecrawl_qdrant_storage/   → /qdrant/storage             │
│      (Vector index)              (Qdrant - External)            │
│      └─ RISK: LOW (reproducible)                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Health Check Status Matrix

```
┌───────────────────────────────────────────────────────────────────┐
│                   HEALTH CHECK CONFIGURATION STATUS              │
├────────────────────────┬──────────┬──────────────────────────────┤
│ Service                │ HC       │ Configuration                │
├────────────────────────┼──────────┼──────────────────────────────┤
│ pulse_mcp          │ ✅ YES   │ GET /health (5s, 30s int)    │
├────────────────────────┼──────────┼──────────────────────────────┤
│ pulse_webhook      │ ✅ YES   │ GET /health (40s, 30s int)   │
├────────────────────────┼──────────┼──────────────────────────────┤
│ firecrawl              │ ❌ NO    │ ⚠️  RECOMMENDED: Add check    │
├────────────────────────┼──────────┼──────────────────────────────┤
│ pulse_postgres           │ ❌ NO    │ ⚠️  RECOMMENDED: pg_isready   │
├────────────────────────┼──────────┼──────────────────────────────┤
│ pulse_redis        │ ❌ NO    │ ⚠️  RECOMMENDED: redis-cli    │
├────────────────────────┼──────────┼──────────────────────────────┤
│ pulse_playwright   │ ❌ NO    │ ⚠️  RECOMMENDED: Add check    │
└────────────────────────┴──────────┴──────────────────────────────┘

Health Check Coverage: 33% (2 of 6 core services)
Recommended Improvement: Add checks to ALL services
```

## Network Communication Patterns

```
┌──────────────────────────────────────────────────────────────┐
│              INTERNAL vs EXTERNAL COMMUNICATION             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  INTERNAL (Docker Network - Use Container Names)            │
│  ================================================            │
│                                                              │
│  From: pulse_mcp                                         │
│  To:   firecrawl (API)                                       │
│  URL:  http://firecrawl:3002  ✅ CORRECT                     │
│  
│  From: firecrawl (API)                                       │
│  To:   pulse_webhook (Webhook)                           │
│  URL:  http://pulse_webhook:52100  ✅ CORRECT            │
│                                                              │
│  From: Any service                                           │
│  To:   PostgreSQL                                            │
│  URL:  postgresql://pulse_postgres:5432/...  ✅ CORRECT        │
│                                                              │
│  ─────────────────────────────────────────────              │
│                                                              │
│  EXTERNAL (Host Machine - Use localhost + Mapped Port)      │
│  ======================================================      │
│                                                              │
│  From: Host Machine / Client Code                           │
│  To:   Firecrawl API                                         │
│  URL:  http://localhost:50102  ✅ CORRECT                    │
│                                                              │
│  From: Host Machine / Browser                               │
│  To:   MCP Server                                            │
│  URL:  http://localhost:50107  ✅ CORRECT                    │
│                                                              │
│  From: Host Machine / Client                                │
│  To:   PostgreSQL                                            │
│  URL:  postgresql://localhost:50105/...  ✅ CORRECT          │
│                                                              │
│  ❌ NEVER USE (ANTI-PATTERNS)                                │
│  ════════════════════════════                               │
│                                                              │
│  ❌ From: container code                                    │
│     To:   http://localhost:50108 (wrong - DNS fails)        │
│                                                              │
│  ❌ From: container code                                    │
│     To:   https://external-api.com (external SSRF)          │
│                                                              │
│  ❌ From: host machine                                      │
│     To:   http://firecrawl:3002 (DNS fails)                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## Environment Variable Namespace Organization

```
┌──────────────────────────────────────────────────────────────┐
│              ENVIRONMENT VARIABLE HIERARCHY                  │
│           (Single Source: Root .env File)                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  INFRASTRUCTURE (SHARED)                                     │
│  ────────────────────────                                   │
│  ├─ POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_PORT         │
│  ├─ REDIS_URL, REDIS_PORT                                   │
│  ├─ PLAYWRIGHT_PORT, PLAYWRIGHT_MICROSERVICE_URL            │
│  └─ APPDATA_BASE (volume mount base)                         │
│                                                              │
│  FIRECRAWL CORE                                              │
│  ──────────────                                              │
│  ├─ FIRECRAWL_PORT, FIRECRAWL_INTERNAL_PORT                 │
│  ├─ FIRECRAWL_API_URL, FIRECRAWL_API_KEY                    │
│  ├─ WORKER_*, SCRAPE_CONCURRENCY, etc.                      │
│  └─ MAX_CPU, MAX_RAM (resource limits)                       │
│                                                              │
│  MCP SERVER                                                  │
│  ──────────                                                  │
│  ├─ MCP_PORT                                                 │
│  ├─ MCP_FIRECRAWL_BASE_URL (http://firecrawl:3002)          │
│  ├─ MCP_FIRECRAWL_API_KEY                                    │
│  ├─ MCP_LLM_* (LLM configuration)                            │
│  ├─ MCP_RESOURCE_* (storage, TTL)                            │
│  └─ MCP_MAP_* (language/region filtering)                    │
│                                                              │
│  WEBHOOK BRIDGE (SEARCH)                                     │
│  ────────────────────────                                   │
│  ├─ WEBHOOK_PORT, WEBHOOK_HOST                              │
│  ├─ WEBHOOK_DATABASE_URL (postgresql://...)                 │
│  ├─ WEBHOOK_REDIS_URL (redis://...)                         │
│  ├─ WEBHOOK_QDRANT_URL, WEBHOOK_TEI_URL                     │
│  ├─ WEBHOOK_HYBRID_ALPHA, WEBHOOK_BM25_*                    │
│  └─ WEBHOOK_SECRET, WEBHOOK_CORS_ORIGINS                    │
│                                                              │
│  SEARCH INTEGRATION (FIRECRAWL → WEBHOOK)                    │
│  ──────────────────────────────────────────                 │
│  ├─ ENABLE_SEARCH_INDEX                                     │
│  ├─ SEARCH_SERVICE_URL (http://localhost:50108)             │
│  ├─ SEARCH_SERVICE_API_SECRET                               │
│  ├─ SELF_HOSTED_WEBHOOK_URL                                 │
│  ├─ SELF_HOSTED_WEBHOOK_HMAC_SECRET                         │
│  └─ ALLOW_LOCAL_WEBHOOKS                                    │
│                                                              │
│  EXTERNAL SERVICES                                           │
│  ──────────────────                                          │
│  ├─ TEI_PORT, WEBHOOK_TEI_URL                               │
│  ├─ QDRANT_HTTP_PORT, QDRANT_GRPC_PORT                      │
│  ├─ WEBHOOK_QDRANT_URL                                      │
│  └─ WEBHOOK_EMBEDDING_MODEL                                 │
│                                                              │
│  AI / LLM                                                    │
│  ────────                                                    │
│  ├─ OPENAI_API_KEY, OPENAI_BASE_URL                         │
│  ├─ ANTHROPIC_API_KEY                                       │
│  ├─ MODEL_NAME                                              │
│  └─ MCP_LLM_MODEL                                            │
│                                                              │
│  APPLICATION SETTINGS                                        │
│  ─────────────────────                                       │
│  ├─ USE_DB_AUTHENTICATION                                    │
│  ├─ LOGGING_LEVEL                                            │
│  └─ DEBUG                                                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

**Legend:**
- ✅ = Configured/Recommended
- ❌ = Not recommended/Anti-pattern
- ⚠️  = Recommended improvement
- 🚀 = Ready to implement
